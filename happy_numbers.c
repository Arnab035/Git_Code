#include<stdio.h>

int main(){
    long int number,new;
    int count=1;
    scanf("%ld",&number);
    while(number!=0){
                     a = number%10;
                     number = number/10;
                     new+=a*a;
                    }
                    
    }
